{
  "name": "@workspace/database",
  "version": "0.0.1",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "DATABASE_URL=\"${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/architecture_playground?schema=public}\" prisma generate",
    "db:migrate": "DATABASE_URL=\"${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/architecture_playground?schema=public}\" prisma migrate dev"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.4.1",
    "@prisma/client": "^7.4.1",
    "@prisma/client-runtime-utils": "^7.4.1",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "prisma": "^7.4.1",
    "typescript": "latest"
  }
}
